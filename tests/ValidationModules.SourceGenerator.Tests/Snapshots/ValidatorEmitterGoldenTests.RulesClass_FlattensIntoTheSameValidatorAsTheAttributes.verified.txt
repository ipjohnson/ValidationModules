// ==== GeneratedValidatorRegistration.g.cs ====
// <auto-generated/>
#nullable enable

namespace Microsoft.Extensions.DependencyInjection {
    using global::Microsoft.Extensions.DependencyInjection.Extensions;
    using global::ValidationModules;
    using global::ValidationModules.Naming;

    /// <summary>Registers every validator this assembly generated.</summary>
    public static class GeneratorTestsValidationExtensions {

        /// <summary>
        /// Adds this assembly's generated validators, and a validation runner for each
        /// validated type.
        /// </summary>
        /// <remarks>
        /// <b>Not idempotent.</b> Calling this twice registers every validator twice, and a
        /// runner merges every registered validator for a type - so each error would be
        /// reported twice. Add rather than TryAdd is deliberate: registering a second
        /// validator for one type is how a hand-written rule composes with the generated
        /// one, so this cannot dedupe without breaking that.
        /// </remarks>
        public static IServiceCollection AddGeneratorTestsValidators(this IServiceCollection services) {
            services.AddSingleton<IValidatorFor<global::Sample.Reservation>, global::Sample.ReservationValidator>();

            services.AddValidationRunner<global::Sample.Reservation>();

            // TryAdd, so a namer the consumer registered first survives. The policy here is
            // the one the literals above were emitted with, so the engines that resolve a
            // namer at run time agree with the generated code by default.
            services.TryAddSingleton<IValidationFieldNamer>(CamelCaseFieldNamer.Instance);

            return services;
        }
    }
}


// ==== Sample.ReservationRules_Rules.g.cs ====
// <auto-generated/>
#nullable enable

using System;
using System.Collections.Generic;
using ValidationModules;

namespace Sample;

/// <summary>
/// The Ensure predicates of ReservationRules, as static methods.
/// </summary>
internal static class ReservationRules_Rules {

    public static bool Rule0(global::Sample.Reservation x) =>
        x.Start < x.End;
}


// ==== Sample.ReservationValidator.g.cs ====
// <auto-generated/>
#nullable enable

using System.Text.RegularExpressions;
using ValidationModules;

namespace Sample;

public sealed partial class ReservationValidator : IValidatorFor<global::Sample.Reservation> {

    public ReservationValidator() { }

    public void Validate(ref ValidationContext ctx, global::Sample.Reservation value) {
        var missingGuest = string.IsNullOrWhiteSpace(value.Guest);
        if (missingGuest) ctx.AddRequired("guest");
        if (!missingGuest && (value.Guest is not null && (value.Guest.Length < 2 || value.Guest.Length > 40))) ctx.AddStringLength("guest", 2, 40);
        if ((value.Nights < 1 || value.Nights > 30)) ctx.AddRange("nights", 1, 30);
        if (!global::Sample.ReservationRules_Rules.Rule0(value)) ctx.Add("start", ValidationCodes.Predicate, "start < end.");
    }

    /// <summary>
    /// The same tests as <see cref="Validate"/>, returning at the first failure and building
    /// no path, message or error record - a caller wanting only a boolean pays for nothing else.
    /// </summary>
    public bool IsValid(global::Sample.Reservation value) {
        if (string.IsNullOrWhiteSpace(value.Guest)) return false;
        if (value.Guest is not null && (value.Guest.Length < 2 || value.Guest.Length > 40)) return false;
        if ((value.Nights < 1 || value.Nights > 30)) return false;
        if (!global::Sample.ReservationRules_Rules.Rule0(value)) return false;
        return true;
    }
}
