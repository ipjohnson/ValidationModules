// <auto-generated/>
#nullable enable
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using ValidationModules;

namespace Sample;

/// <summary>
/// The transcribed Describe body of PetRules: read from the rules class, run from here.
/// </summary>
internal static class PetRules_Rules
{

    public static global::ValidationModules.ValidationFlow Describe(ref global::ValidationModules.ValidationContext ctx, global::Sample.Pet x)
    {
        var count = 0;
        if (x.Age > 1)
        {
            var missingName = string.IsNullOrWhiteSpace(x.Name);
            if (missingName && global::ValidationModules.ValidationContextExtensions.ReportRequired(ctx, "name").ShouldStop)
            {
                return global::ValidationModules.ValidationFlow.Stop;
            }
            if (!missingName && (x.Name is not null && (x.Name.Length < 2 || x.Name.Length > 40)) && global::ValidationModules.ValidationContextExtensions.ReportStringLength(ctx, "name", 2, 40).ShouldStop)
            {
                return global::ValidationModules.ValidationFlow.Stop;
            }
        }
        else if (x.Age < 0)
        {
            if ((x.Age < 0 || x.Age > 30) && global::ValidationModules.ValidationContextExtensions.ReportRange(ctx, "age", 0, 30).ShouldStop)
            {
                return global::ValidationModules.ValidationFlow.Stop;
            }
        }
        else
        {
            count++;
        }
        switch (x.Age)
        {
            case 1:
            case 2:
                if (string.IsNullOrWhiteSpace(x.Name) && global::ValidationModules.ValidationContextExtensions.ReportRequired(ctx, "name").ShouldStop)
                {
                    return global::ValidationModules.ValidationFlow.Stop;
                }
                break;
            default:
                count--;
                break;
        }
        for (var i = 0; i < 3; i++)
        {
            count += i;
        }
        foreach (string tag in x.Tags ?? new List<string>())
        {
            if ((ctx.Report("tags", "tag", tag)).ShouldStop)
            {
                return global::ValidationModules.ValidationFlow.Stop;
            }
        }
        while (count > 10)
        {
            count--;
        }
        do
        {
            count++;
        }
        while (count < 2);
        {
            var copy = count;
            count = copy + 1;
        }
        int Twice(int value)
        {
            if (value > 100)
            {
                return 0;
            }
            using (var reader = new StringReader(x.Name ?? ""))
            {
                value += reader.Read();
            }
            try
            {
                checked
                {
                    value *= 2;
                }
            }
            catch (OverflowException)
            {
                value = 0;
            }
            finally
            {
                count++;
            }
            return value;
        }
        if (x.Tags?.Any(tag =>
        {
            return tag.Length > Twice(count);
        }) == true)
        {
            if (string.IsNullOrWhiteSpace(x.Name) && global::ValidationModules.ValidationContextExtensions.ReportRequired(ctx, "name").ShouldStop)
            {
                return global::ValidationModules.ValidationFlow.Stop;
            }
        }
        if (x.Tags is not null && (x.Tags.Count > 5) && global::ValidationModules.ValidationContextExtensions.ReportItemCount(ctx, "tags", 0, 5).ShouldStop)
        {
            return global::ValidationModules.ValidationFlow.Stop;
        }
        if (x.Tags is { } items0)
        {
            for (var i0 = 0; i0 < items0.Count; i0++)
            {
                var element0 = items0[i0];
                if (element0 is not null && (element0.Length < 1 || element0.Length > 20) && global::ValidationModules.ValidationContextExtensions.ReportStringLength(ctx, $"tags[{i0}]", 1, 20).ShouldStop)
                {
                    return global::ValidationModules.ValidationFlow.Stop;
                }
            }
        }
        if (!(x.Age >= count) && ctx.Report("age", "age_count", "age >= count.").ShouldStop)
        {
            return global::ValidationModules.ValidationFlow.Stop;
        }
        return global::ValidationModules.ValidationFlow.Continue;
    }
}
