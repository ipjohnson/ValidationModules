// ==== GeneratedValidatorRegistration.g.cs ====
// <auto-generated/>
#nullable enable

using System;
using System.Collections.Generic;
using ValidationModules;

namespace GeneratorTests;

/// <summary>Every validator this assembly generated, ready for AddValidationModules.</summary>
public static class GeneratedValidators {
    public static IReadOnlyList<ValidatorRegistration> All { get; } = new ValidatorRegistration[] {
        new ValidatorRegistration(typeof(IValidatorFor<global::Sample.Pet>), static _ => global::Sample.PetValidator.Instance),
    };
}


// ==== Sample.PetValidator.g.cs ====
// <auto-generated/>
#nullable enable

using System.Text.RegularExpressions;
using ValidationModules;

namespace Sample;

public sealed partial class PetValidator : IValidatorFor<global::Sample.Pet> {
    public static readonly PetValidator Instance = new();

    private PetValidator() { }

    private static readonly Regex SkuPattern0 = new Regex("^[A-Z]{3}$");

    public void Validate(ref ValidationContext ctx, global::Sample.Pet value) {
        if (string.IsNullOrWhiteSpace(value.Name)) ctx.AddRequired("name");
        else if (value.Name is not null && (value.Name.Length < 1 || value.Name.Length > 100)) ctx.AddStringLength("name", 1, 100);
        if ((value.Age < 0 || value.Age > 30)) ctx.AddRange("age", 0, 30);
        if (value.Sku is not null && !SkuPattern0.IsMatch(value.Sku)) ctx.AddPattern("sku");
        if (value.Status is not null && (value.Status != "available" && value.Status != "pending" && value.Status != "sold")) ctx.AddAllowedValues("status", "available, pending, sold");
        if (value.Tags is not null && (value.Tags.Count < 1 || value.Tags.Count > 10)) ctx.AddItemCount("tags", 1, 10);
    }
}
