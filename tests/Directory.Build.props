<Project>

    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))"/>

    <!-- Nothing under tests/ ships to NuGet. -->
    <PropertyGroup>
        <IsPackable>false</IsPackable>
    </PropertyGroup>

</Project>
